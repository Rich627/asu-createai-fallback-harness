"""The implementation. The scripts in the repository root are the entry points.

Nothing in this package imports from those scripts: the dependency runs one way, root -> asu,
so the package stays importable on its own and the entry points stay thin.
"""
